import { SearchPurchasePipe } from './search-purchase.pipe';

describe('SearchPurchasePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPurchasePipe();
    expect(pipe).toBeTruthy();
  });
});
